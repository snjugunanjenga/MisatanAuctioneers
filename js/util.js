// Utility function: Check if an email is valid (basic regex)
function isValidEmail(email) {
    const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return regex.test(email);
  }
  
  // Future helper functions (e.g., slider utilities) can be added here.
  

  